import { View, Text } from 'react-native'
import React from 'react'
import { createNativeStackNavigator } from '@react-navigation/native-stack';

export default function MainPage({ navigation }) {
  return (
    <View style={{top:400, left:170}}>
      <Text>MainPage</Text>
    </View>
  )
}